{% extends 'base.html.twig' %}

{% block title teacher.title %}

{% block body %}
    <article>
    <h1>{{ teacher.title}} </h1>
    <p>{{ pin.description}} </p>
    </article>

    <a href="{{  path('/teachers) }}>Go Back</a>

{% endblock %}