{% extends 'layouts/base.html.twig' %}

{% block title 'New Teacher'%}

{% block body %}


            <div class="row">
                <div class="col-md-6 mx-auto">

                    <h1>Create a new Teacher</h1>
                        {{ form_start(form) }}
                            {{ form_widget(form) }}

                                <input type="submit" class="btn btn-primary btn-lg btn-block" value="Create teacher" formnovalidate>

                        {{ form_end(form) }}
                
                </div>
            </div>

{% endblock %}
